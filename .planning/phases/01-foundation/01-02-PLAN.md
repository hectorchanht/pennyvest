---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/strategies/types.ts
  - src/lib/strategies/future-tech.ts
  - src/lib/strategies/traditional.ts
  - src/lib/strategies/commodities.ts
  - src/lib/strategies/crypto.ts
  - src/lib/strategies/index.ts
  - src/lib/strategies/profiles.ts
  - src/lib/data/server-guard.ts
  - .env.local.example
  - .env.local
autonomous: true
requirements:
  - DATA-04

must_haves:
  truths:
    - "All four strategy configurations exist as importable TypeScript modules with tickers, weights, risk levels, and i18n keys"
    - "Three allocation profiles (Conservative, Balanced, Aggressive) distribute weights across the four funds"
    - "No API key or credential is accessible in the browser JS bundle"
    - "The server-only pattern is established so future lib/data/ files will fail at build time if imported by client components"
    - "The project builds with TypeScript strict mode passing"
  artifacts:
    - path: "src/lib/strategies/types.ts"
      provides: "Strategy, Allocation, AllocationProfile, RiskLevel, AssetClass types"
      exports: ["Strategy", "Allocation", "AllocationProfile", "RiskLevel", "AssetClass"]
    - path: "src/lib/strategies/future-tech.ts"
      provides: "Future Tech fund config with ~6 holdings"
      exports: ["futureTechStrategy"]
    - path: "src/lib/strategies/traditional.ts"
      provides: "Traditional Industries fund config"
      exports: ["traditionalStrategy"]
    - path: "src/lib/strategies/commodities.ts"
      provides: "Commodities fund config"
      exports: ["commoditiesStrategy"]
    - path: "src/lib/strategies/crypto.ts"
      provides: "Crypto fund config"
      exports: ["cryptoStrategy"]
    - path: "src/lib/strategies/index.ts"
      provides: "Registry with getStrategyConfig and getAllStrategies"
      exports: ["strategies", "getStrategyConfig", "getAllStrategies"]
    - path: "src/lib/strategies/profiles.ts"
      provides: "3 allocation profiles mapping fund slugs to weights"
      exports: ["profiles", "getProfile", "getAllProfiles"]
    - path: "src/lib/data/server-guard.ts"
      provides: "server-only import to establish the pattern for lib/data/"
      contains: "import 'server-only'"
    - path: ".env.local.example"
      provides: "Template of all planned env vars with empty values (committed)"
  key_links:
    - from: "src/lib/strategies/future-tech.ts"
      to: "src/lib/strategies/types.ts"
      via: "import type { Strategy }"
      pattern: "import.*Strategy.*from.*types"
    - from: "src/lib/strategies/index.ts"
      to: "src/lib/strategies/future-tech.ts"
      via: "import and register all 4 strategies"
      pattern: "import.*futureTechStrategy"
    - from: "src/lib/strategies/profiles.ts"
      to: "src/lib/strategies/types.ts"
      via: "import AllocationProfile type"
      pattern: "import.*AllocationProfile"
    - from: "src/lib/data/server-guard.ts"
      to: "server-only"
      via: "import 'server-only' enforces build-time boundary"
      pattern: "import 'server-only'"
---

<objective>
Create the strategy configuration system and establish server-only API key hygiene.

Purpose: Strategy configs are the authoritative data source that every page in Phases 2-4 consumes. The server-only pattern prevents accidental credential leaks in all future data-fetching code.
Output: 4 fund configs, 3 allocation profiles, and the server-only enforcement pattern.
</objective>

<execution_context>
@/Users/laichan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laichan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- From Plan 01 — locale routing and translation structure already established -->

From src/i18n/routing.ts (created in Plan 01):
```typescript
export const routing = defineRouting({
  locales: ['en', 'zh-HK'],
  defaultLocale: 'en',
});
```

From src/messages/en.json (created in Plan 01):
```json
{
  "strategies": {
    "futureTech": { "name": "Future Tech", "tagline": "...", "rationale": "..." },
    "traditional": { "name": "Traditional Industries", ... },
    "commodities": { "name": "Commodities", ... },
    "crypto": { "name": "Crypto", ... }
  },
  "profiles": {
    "conservative": { "name": "Conservative" },
    "balanced": { "name": "Balanced" },
    "aggressive": { "name": "Aggressive" }
  },
  "riskLevel": { "low": "Low Risk", "medium": "Medium Risk", "high": "High Risk" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create strategy types, fund configs, and allocation profiles</name>
  <files>
    src/lib/strategies/types.ts, src/lib/strategies/future-tech.ts,
    src/lib/strategies/traditional.ts, src/lib/strategies/commodities.ts,
    src/lib/strategies/crypto.ts, src/lib/strategies/index.ts,
    src/lib/strategies/profiles.ts
  </files>
  <action>
    1. **Create src/lib/strategies/types.ts** with the exact types from 01-RESEARCH.md Pattern 3:
       - `RiskLevel = 'low' | 'medium' | 'high'`
       - `AssetClass = 'equity' | 'etf' | 'commodity' | 'crypto'`
       - `Allocation` interface: ticker, name, weight (0-1 decimal), assetClass
       - `Strategy` interface: slug, riskLevel, allocations[], nameKey (i18n), rationaleKey (i18n)
       - `AllocationProfile` interface: slug, nameKey, weights (Record of fundSlug to weight 0-1)

    2. **Create the 4 fund config files** (Claude's discretion on specific tickers/weights per user decision):

       **src/lib/strategies/future-tech.ts** — riskLevel: 'high'
       ~6 holdings mixing individual tech stocks and ETFs. Suggestions from research: NVDA, TSLA, ARKK, AMD, PLTR, MSTR. Weights must sum to 1.0. nameKey: 'strategies.futureTech.name', rationaleKey: 'strategies.futureTech.rationale'.

       **src/lib/strategies/traditional.ts** — riskLevel: 'low'
       ~6 holdings in F&B, financials, real estate (per PROJECT.md context). Mix of individual stocks and ETFs. Examples: JNJ, PG, JPM, VNQ, O, KO. Weights must sum to 1.0.

       **src/lib/strategies/commodities.ts** — riskLevel: 'medium'
       ~6 holdings in gold, oil, agricultural commodities. Mix of commodity ETFs and individual plays. Examples: GLD, USO, DBA, BHP, NEM, WEAT. Weights must sum to 1.0.

       **src/lib/strategies/crypto.ts** — riskLevel: 'high'
       ~6 holdings of major coins and emerging tokens. Use CoinGecko-compatible ids where possible. Examples: BTC (bitcoin), ETH (ethereum), SOL (solana), LINK (chainlink), AVAX, DOT. assetClass: 'crypto' for all. Weights must sum to 1.0.

    3. **Create src/lib/strategies/index.ts** — registry pattern from 01-RESEARCH.md:
       - Import all 4 strategies
       - Export `strategies: Record<string, Strategy>` keyed by slug
       - Export `getStrategyConfig(slug: string): Strategy | undefined`
       - Export `getAllStrategies(): Strategy[]`

    4. **Create src/lib/strategies/profiles.ts** — 3 allocation profiles (Claude's discretion on exact percentages):

       **Conservative:** Higher weight to Traditional Industries and Commodities, lower to Crypto and Future Tech. Example: traditional 0.40, commodities 0.30, future-tech 0.20, crypto 0.10.

       **Balanced:** Roughly even distribution with slight tilt toward growth. Example: future-tech 0.30, traditional 0.25, commodities 0.25, crypto 0.20.

       **Aggressive:** Heavy on Future Tech and Crypto, lighter on defensive. Example: future-tech 0.35, crypto 0.35, commodities 0.15, traditional 0.15.

       Export: `profiles: Record<string, AllocationProfile>`, `getProfile(slug)`, `getAllProfiles()`.

    5. **Validate all weights sum to 1.0** in each fund config and each profile. Use exact decimals (not floating point arithmetic). TypeScript strict mode will catch type mismatches.

    IMPORTANT: Each strategy's nameKey and rationaleKey must match the keys already in src/messages/en.json and src/messages/zh-HK.json (created in Plan 01). E.g., futureTech strategy uses nameKey: 'strategies.futureTech.name'.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/pennyvest/v1 && pnpm build</automated>
  </verify>
  <done>
    - All 4 fund config files exist with typed Strategy objects
    - Each fund has ~6 holdings with weights summing to 1.0
    - 3 allocation profiles exist with weights summing to 1.0
    - Registry exports getStrategyConfig and getAllStrategies
    - TypeScript strict mode passes (no type errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Establish server-only pattern and env var hygiene</name>
  <files>
    src/lib/data/server-guard.ts, .env.local.example, .env.local, .gitignore
  </files>
  <action>
    1. **Create src/lib/data/server-guard.ts** — a minimal file that establishes the server-only import pattern for the lib/data/ directory:
       ```typescript
       import 'server-only';
       // This file establishes the server-only boundary for src/lib/data/.
       // Every file in this directory must include `import 'server-only'` at the top.
       // This causes a build error if any file here is accidentally imported by a client component.
       ```
       This file serves as the pattern anchor. In Phase 3, actual data-fetching modules (yahoo-finance, coingecko, news) will each include `import 'server-only'` at the top.

    2. **Create .env.local.example** (committed to git — safe, no values):
       ```
       # Upstash Redis (server-only — Phase 3)
       UPSTASH_REDIS_REST_URL=
       UPSTASH_REDIS_REST_TOKEN=

       # OpenAI (server-only — Phase 3)
       OPENAI_API_KEY=

       # NewsAPI (server-only — Phase 3)
       NEWS_API_KEY=

       # Google Analytics (client-safe — NEXT_PUBLIC_ prefix)
       NEXT_PUBLIC_GA_MEASUREMENT_ID=
       ```

    3. **Create .env.local** (gitignored — empty values for now):
       Copy the same structure as .env.local.example with empty values. The user will fill these in when signing up for services in later phases.

    4. **Verify .gitignore** includes `.env.local` and `.env*.local` (create-next-app should have added these, but confirm). Also confirm it does NOT include `.env.local.example`.

    5. **Verify no NEXT_PUBLIC_ prefix on any server-side env vars** by running:
       ```
       grep -r "NEXT_PUBLIC_" .env.local.example
       ```
       Only `NEXT_PUBLIC_GA_MEASUREMENT_ID` should appear — all API keys (Upstash, OpenAI, NewsAPI) must NOT have the prefix.

    6. **Run final build** to confirm the entire project — scaffold + i18n + strategies + server-only — compiles cleanly.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/pennyvest/v1 && pnpm build && grep -r "NEXT_PUBLIC_" .env.local.example | grep -v "GA_MEASUREMENT_ID" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>
    - src/lib/data/server-guard.ts exists with `import 'server-only'`
    - .env.local.example exists and is committed (no secret values)
    - .env.local exists and is gitignored
    - No API key env var uses NEXT_PUBLIC_ prefix (only GA does)
    - Full project build passes with zero errors
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with zero errors
2. `grep -r "NEXT_PUBLIC_" .env.local.example` returns only GA_MEASUREMENT_ID line
3. `git status` shows .env.local is NOT tracked (gitignored)
4. All 4 strategy files importable: `import { futureTechStrategy } from '@/lib/strategies/future-tech'` compiles
5. `getAllStrategies()` returns 4 strategies, `getAllProfiles()` returns 3 profiles
6. server-guard.ts contains `import 'server-only'`
</verification>

<success_criteria>
- 4 fund configs with typed holdings and weights summing to 1.0
- 3 allocation profiles distributing weights across funds
- server-only enforcement pattern established in lib/data/
- All env vars properly classified (server-only vs NEXT_PUBLIC_)
- Clean build with TypeScript strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
