---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - tsconfig.json
  - eslint.config.mjs
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/app/layout.tsx
  - src/proxy.ts
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/i18n/navigation.ts
  - src/messages/en.json
  - src/messages/zh-HK.json
  - .gitignore
  - .prettierrc
autonomous: true
requirements:
  - I18N-04

must_haves:
  truths:
    - "Navigating to /en/ renders an English page without errors"
    - "Navigating to /zh/ renders a Traditional Chinese page without errors"
    - "Navigating to / redirects to /en/ or /zh/ based on browser language"
    - "The project builds with zero TypeScript and ESLint errors"
    - "Dark theme with green/teal brand colors is applied"
  artifacts:
    - path: "src/proxy.ts"
      provides: "next-intl locale routing middleware (Next.js 16 proxy pattern)"
      contains: "createMiddleware"
    - path: "src/i18n/routing.ts"
      provides: "Locale routing config with en and zh-HK"
      contains: "defineRouting"
    - path: "src/i18n/request.ts"
      provides: "Server-side message loading per locale"
      contains: "getRequestConfig"
    - path: "src/i18n/navigation.ts"
      provides: "Locale-aware Link, redirect, usePathname, useRouter"
      contains: "createNavigation"
    - path: "src/messages/en.json"
      provides: "English translation keys for navigation, strategies, profiles, risk levels"
    - path: "src/messages/zh-HK.json"
      provides: "Traditional Chinese translation keys"
    - path: "src/app/[locale]/layout.tsx"
      provides: "Locale layout with NextIntlClientProvider and lang attribute"
      contains: "NextIntlClientProvider"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 theme with dark background and green/teal brand tokens"
      contains: "@theme"
  key_links:
    - from: "src/proxy.ts"
      to: "src/i18n/routing.ts"
      via: "import routing config"
      pattern: "import.*routing"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/i18n/routing.ts"
      via: "locale validation"
      pattern: "hasLocale.*routing\\.locales"
    - from: "src/app/[locale]/page.tsx"
      to: "src/messages/en.json"
      via: "getTranslations loads messages"
      pattern: "getTranslations"
---

<objective>
Bootstrap the Next.js 16 project with bilingual routing fully operational.

Purpose: This is the skeleton every subsequent phase builds on. Without working locale routing and a clean build, nothing else can proceed.
Output: A running Next.js 16 app where /en/ and /zh/ render correct locale content, with Tailwind v4 dark theme configured.
</objective>

<execution_context>
@/Users/laichan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laichan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js 16 with next-intl bilingual routing</name>
  <files>
    package.json, pnpm-lock.yaml, next.config.ts, tsconfig.json, eslint.config.mjs,
    postcss.config.mjs, .gitignore, .prettierrc,
    src/proxy.ts, src/i18n/routing.ts, src/i18n/request.ts, src/i18n/navigation.ts,
    src/messages/en.json, src/messages/zh-HK.json,
    src/app/layout.tsx, src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx
  </files>
  <action>
    1. **Scaffold the project** from the project root `/Users/laichan/code/pennyvest/v1`:
       ```
       pnpm create next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*"
       ```
       If prompted about existing files, accept overwrite (only .git and .planning exist).

    2. **Install i18n and server-only packages:**
       ```
       pnpm add next-intl server-only
       pnpm add -D prettier eslint-config-prettier
       ```

    3. **Configure next.config.ts** with next-intl plugin:
       ```typescript
       import type { NextConfig } from 'next';
       import createNextIntlPlugin from 'next-intl/plugin';
       const withNextIntl = createNextIntlPlugin();
       const nextConfig: NextConfig = {};
       export default withNextIntl(nextConfig);
       ```

    4. **Verify tsconfig.json** has `"strict": true` and add `"noUncheckedIndexedAccess": true` if not present. Ensure `"paths": { "@/*": ["./src/*"] }` is set.

    5. **Create src/i18n/routing.ts:**
       ```typescript
       import { defineRouting } from 'next-intl/routing';
       export const routing = defineRouting({
         locales: ['en', 'zh-HK'],
         defaultLocale: 'en',
       });
       ```

    6. **Create src/i18n/request.ts** — use the exact pattern from 01-RESEARCH.md Pattern 1 (getRequestConfig with hasLocale check and dynamic import of messages).

    7. **Create src/i18n/navigation.ts** — use createNavigation(routing) exporting Link, redirect, usePathname, useRouter, getPathname.

    8. **Create src/proxy.ts** (NOT middleware.ts — Next.js 16 requires proxy.ts):
       ```typescript
       import createMiddleware from 'next-intl/middleware';
       import { routing } from './i18n/routing';
       export default createMiddleware(routing);
       export const config = {
         matcher: '/((?!api|trpc|_next|_vercel|.*\\..*).*)',
       };
       ```

    9. **Create src/messages/en.json** with the full structure from 01-RESEARCH.md Pattern 6 — navigation, strategies (all 4 funds with name/tagline/rationale placeholder), profiles (conservative/balanced/aggressive), riskLevel keys.

    10. **Create src/messages/zh-HK.json** with the full Traditional Chinese structure from 01-RESEARCH.md Pattern 6 — using formal financial terms: 未來科技, 傳統行業, 大宗商品, 加密貨幣 for fund names; 保守型, 平衡型, 進取型 for profiles.

    11. **Create src/app/[locale]/layout.tsx** — use the exact async params pattern from 01-RESEARCH.md. CRITICAL: `params` is `Promise<{ locale: string }>` and must be awaited. Include NextIntlClientProvider, setRequestLocale, hasLocale check, generateStaticParams, html lang attribute.

    12. **Update src/app/[locale]/page.tsx** — simple placeholder page that uses `getTranslations('navigation')` to display a heading proving i18n works. Must await params and call setRequestLocale. Show the fund names from translations to prove both locales load.

    13. **Remove src/app/page.tsx** if create-next-app generated one at the root level (next-intl routes everything through [locale]).

    14. **Ensure src/app/layout.tsx** (root layout) exists as a minimal wrapper — it should NOT set lang attribute (that's the locale layout's job). It should import globals.css.

    15. **Add .prettierrc** with basic config: `{ "semi": true, "singleQuote": true, "trailingComma": "all" }`.

    16. **Update package.json scripts:**
        - `"lint": "eslint ."` (NOT `next lint` — removed in Next.js 16)
        - `"format": "prettier --write ."`
        - Keep existing `dev`, `build`, `start` scripts.

    17. **Add eslint-config-prettier** to the eslint.config.mjs flat config to disable conflicting Prettier rules.

    CRITICAL ANTI-PATTERNS TO AVOID:
    - Do NOT create src/middleware.ts — must be src/proxy.ts
    - Do NOT use synchronous params access — always await params
    - Do NOT use `export const runtime = 'edge'` in proxy.ts
    - Do NOT use `next lint` in scripts — use `eslint .`
    - Do NOT create tailwind.config.js — Tailwind v4 uses CSS-first @theme
    - Do NOT use zh-CN or zh — locale must be zh-HK
  </action>
  <verify>
    <automated>cd /Users/laichan/code/pennyvest/v1 && pnpm build</automated>
  </verify>
  <done>
    - `pnpm build` passes with zero TypeScript errors and zero ESLint errors
    - src/proxy.ts exists (not middleware.ts)
    - src/i18n/routing.ts defines locales ['en', 'zh-HK']
    - src/messages/en.json and src/messages/zh-HK.json contain all 4 strategy translation keys
    - src/app/[locale]/layout.tsx uses async params pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v4 dark theme with Pennyvest brand tokens</name>
  <files>src/app/globals.css</files>
  <action>
    Replace the default globals.css content generated by create-next-app with Pennyvest's dark theme configuration.

    1. **Set up Tailwind v4 CSS-first config in src/app/globals.css:**
       ```css
       @import "tailwindcss";

       @theme {
         --color-brand-green: oklch(0.72 0.17 155);
         --color-brand-teal: oklch(0.70 0.14 190);
         --color-brand-green-light: oklch(0.82 0.14 155);
         --color-brand-green-dark: oklch(0.55 0.17 155);
         --color-background: oklch(0.13 0.01 240);
         --color-surface: oklch(0.18 0.01 240);
         --color-surface-hover: oklch(0.22 0.01 240);
         --color-text-primary: oklch(0.95 0.01 240);
         --color-text-secondary: oklch(0.70 0.01 240);
         --color-text-muted: oklch(0.50 0.01 240);
         --color-border: oklch(0.25 0.01 240);
         --font-family-sans: 'Inter', 'Noto Sans TC', system-ui, sans-serif;
       }
       ```

    2. **Add base styles** below the @theme block:
       ```css
       body {
         background-color: var(--color-background);
         color: var(--color-text-primary);
         font-family: var(--font-family-sans);
       }
       ```

    3. **Do NOT create tailwind.config.js** — this is Tailwind v4 CSS-first configuration.

    4. **Include Inter and Noto Sans TC fonts** — add `@import` for Google Fonts at the top of globals.css, or configure via next/font in the root layout. Prefer next/font/google for performance (automatic subsetting and self-hosting). If using next/font, import Inter and Noto_Sans_TC in the root layout and apply via CSS variable.

    The theme is dark by default (no light mode toggle — locked decision: "v1 ships dark only"). Green/teal brand colors per user decision. Noto Sans TC is required for Traditional Chinese glyph support.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/pennyvest/v1 && pnpm build</automated>
  </verify>
  <done>
    - globals.css uses @theme block (not tailwind.config.js)
    - Brand colors (green/teal) defined as CSS custom properties
    - Dark background color applied to body
    - Font family includes both Inter (Latin) and Noto Sans TC (Traditional Chinese)
    - Build still passes
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with zero errors
2. `pnpm dev` — navigate to http://localhost:3000/en/ — renders English content
3. `pnpm dev` — navigate to http://localhost:3000/zh/ — renders Traditional Chinese content (未來科技 visible)
4. Navigate to http://localhost:3000/ — redirects to /en/ (default locale)
5. Page background is dark, text is light, font renders correctly for both EN and zh-HK
6. No tailwind.config.js file exists
7. No src/middleware.ts file exists (only src/proxy.ts)
</verification>

<success_criteria>
- The Next.js 16 app builds and runs with bilingual routing at /en/ and /zh/
- Tailwind v4 dark theme with green/teal brand tokens is applied
- All next-intl files follow Next.js 16 patterns (proxy.ts, async params)
- Translation files contain all 4 strategy names in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
