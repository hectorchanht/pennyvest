---
phase: 03-live-data-charts-and-ai
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/charts/AllocationDonut.tsx
  - src/components/charts/EquityCurve.tsx
  - src/messages/en.json
  - src/messages/zh-HK.json
autonomous: true
requirements: [STRT-02, STRT-06]

must_haves:
  truths:
    - "Donut chart renders allocation breakdown with inner radius (hole) per D-01"
    - "Equity curve renders as a line/area chart labeled 'Simulated back-test' per D-02"
    - "Both charts are client components with 'use client' directive"
    - "i18n keys exist for all chart labels, error states, and news UI in both languages"
  artifacts:
    - path: "src/components/charts/AllocationDonut.tsx"
      provides: "Donut chart for allocation breakdown"
      exports: ["default (AllocationDonut)"]
      contains: "use client"
    - path: "src/components/charts/EquityCurve.tsx"
      provides: "Area/line chart for simulated equity curve"
      exports: ["default (EquityCurve)"]
      contains: "use client"
  key_links:
    - from: "src/components/charts/AllocationDonut.tsx"
      to: "recharts"
      via: "PieChart import"
      pattern: "import.*PieChart.*recharts"
    - from: "src/components/charts/EquityCurve.tsx"
      to: "recharts"
      via: "AreaChart import"
      pattern: "import.*AreaChart.*recharts"
---

<objective>
Build the two chart client components (donut allocation chart and equity curve) and add all Phase 3 i18n translation keys for charts, news, errors, and loading states.

Purpose: Charts are independent client components that receive data as props. They need no server-side data adapters -- just types. Building them in parallel with data adapters (Plan 02) maximizes velocity.
Output: Two chart components in src/components/charts/ and updated i18n messages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-live-data-charts-and-ai/03-CONTEXT.md
@.planning/phases/03-live-data-charts-and-ai/03-RESEARCH.md
@.planning/phases/03-live-data-charts-and-ai/03-01-SUMMARY.md

@src/types/prices.ts
@src/components/strategy/ComingSoonCard.tsx
@src/messages/en.json
@src/messages/zh-HK.json

<interfaces>
From src/types/prices.ts:
```typescript
export interface EquityPoint {
  date: string;
  value: number; // cumulative return as percentage
}
```

From src/lib/strategies/types.ts:
```typescript
export interface Allocation {
  ticker: string;
  name: string;
  weight: number;
  assetClass: AssetClass;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AllocationDonut and EquityCurve chart components</name>
  <files>src/components/charts/AllocationDonut.tsx, src/components/charts/EquityCurve.tsx</files>
  <read_first>
    - src/components/strategy/ComingSoonCard.tsx
    - src/components/strategy/AllocationSidebar.tsx
    - src/types/prices.ts
  </read_first>
  <action>
1. Create `src/components/charts/AllocationDonut.tsx`:
   - Add `'use client'` directive at the very top
   - Import `PieChart, Pie, Cell, Tooltip, ResponsiveContainer` from `recharts`
   - Define color palette: `const COLORS = ['#6366f1', '#22d3ee', '#f59e0b', '#10b981', '#f43f5e', '#a78bfa']` (indigo, cyan, amber, emerald, rose, violet -- works on dark theme)
   - Props interface: `{ allocations: { name: string; ticker: string; weight: number }[]; centerLabel?: string; title: string }`
   - Map allocations to `{ name: ticker, value: weight, fullName: name }` for Recharts data format
   - Render `<div>` wrapper with:
     - `<h3>` with `title` prop, styled `text-sm font-medium text-text-primary mb-3`
     - `<ResponsiveContainer width="100%" height={220}>`
     - `<PieChart>` with `<Pie>` using `innerRadius={65} outerRadius={95} paddingAngle={2} strokeWidth={0}` per D-01 (donut with center hole)
     - `<Cell>` for each entry using COLORS palette
     - `<Tooltip>` with formatter showing percentage: `(value: number) => Math.round(value * 100) + '%'`
   - Center label overlay: absolute-positioned div inside the wrapper showing `centerLabel` if provided (e.g., risk level or fund name)
   - Export as default

2. Create `src/components/charts/EquityCurve.tsx`:
   - Add `'use client'` directive at the very top
   - Import `AreaChart, Area, XAxis, YAxis, Tooltip, CartesianGrid, ResponsiveContainer, ReferenceLine` from `recharts`
   - Import `EquityPoint` from `@/types/prices`
   - Props interface: `{ data: EquityPoint[]; simulatedLabel: string; title: string }`
   - Render `<div>` wrapper with:
     - `<h3>` with `title` prop, styled `text-sm font-medium text-text-primary mb-1`
     - `<p>` with `simulatedLabel` prop (e.g., "Simulated back-test"), styled `text-xs text-text-muted mb-2` -- per D-02, this label MUST be visible in the chart title area
     - `<ResponsiveContainer width="100%" height={180}>`
     - `<AreaChart>` with data prop and margin `{ top: 5, right: 5, bottom: 0, left: 0 }`
     - `<defs>` with `<linearGradient id="equityGradient">` from `#6366f1` opacity 0.3 to opacity 0 (indigo gradient fill)
     - `<CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.05)" />` (subtle grid for dark theme)
     - `<XAxis dataKey="date" tick={{ fontSize: 10 }} tickLine={false} stroke="rgba(255,255,255,0.3)" />` -- show date labels
     - `<YAxis tickFormatter={(v) => (v > 0 ? '+' : '') + v.toFixed(0) + '%'} tick={{ fontSize: 10 }} axisLine={false} stroke="rgba(255,255,255,0.3)" />`
     - `<Tooltip formatter={(v: number) => [v.toFixed(1) + '%', 'Return']} />`
     - `<ReferenceLine y={0} stroke="rgba(255,255,255,0.2)" />` (zero-return baseline)
     - `<Area type="monotone" dataKey="value" stroke="#6366f1" fill="url(#equityGradient)" strokeWidth={1.5} dot={false} />`
   - Export as default
  </action>
  <verify>
    <automated>cd /Users/laichan/code/pennyvest/v1 && npx tsc --noEmit src/components/charts/AllocationDonut.tsx src/components/charts/EquityCurve.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'use client'" src/components/charts/AllocationDonut.tsx
    - grep -q "'use client'" src/components/charts/EquityCurve.tsx
    - grep -q "PieChart" src/components/charts/AllocationDonut.tsx
    - grep -q "innerRadius" src/components/charts/AllocationDonut.tsx
    - grep -q "AreaChart" src/components/charts/EquityCurve.tsx
    - grep -q "simulatedLabel" src/components/charts/EquityCurve.tsx
    - grep -q "equityGradient" src/components/charts/EquityCurve.tsx
    - grep -q "export default" src/components/charts/AllocationDonut.tsx
    - grep -q "export default" src/components/charts/EquityCurve.tsx
  </acceptance_criteria>
  <done>AllocationDonut renders a donut chart (PieChart with innerRadius per D-01) with color-coded segments and tooltip. EquityCurve renders an area chart with "Simulated back-test" label (per D-02), gradient fill, and percentage Y-axis. Both are client components.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 3 i18n translation keys for charts, news, errors, and loading</name>
  <files>src/messages/en.json, src/messages/zh-HK.json</files>
  <read_first>
    - src/messages/en.json
    - src/messages/zh-HK.json
  </read_first>
  <action>
Add the following keys to both message files. Add them under a new top-level section. Do NOT remove any existing keys -- only add new ones.

**English (`en.json`) -- add these keys:**
```json
{
  "charts": {
    "allocationTitle": "Allocation Breakdown",
    "equityTitle": "Performance History",
    "simulatedLabel": "Simulated back-test — not actual returns",
    "loading": "Loading chart data...",
    "error": "Chart data unavailable",
    "retry": "Retry"
  },
  "news": {
    "sectionTitle": "Market News & AI Analysis",
    "impact": {
      "bullish": "Bullish",
      "neutral": "Neutral",
      "bearish": "Bearish"
    },
    "analysisUnavailable": "AI analysis unavailable",
    "noArticles": "No recent news available",
    "loading": "Loading news...",
    "error": "News data unavailable",
    "retry": "Retry",
    "source": "Source"
  },
  "common": {
    "lastUpdated": "Last updated: {time}",
    "staleWarning": "Data may be outdated",
    "price": "Price",
    "change24h": "24h Change"
  }
}
```

**Traditional Chinese (`zh-HK.json`) -- add these keys:**
```json
{
  "charts": {
    "allocationTitle": "資產配置",
    "equityTitle": "歷史表現",
    "simulatedLabel": "模擬回測 — 非實際回報",
    "loading": "正在載入圖表數據...",
    "error": "圖表數據暫時無法使用",
    "retry": "重試"
  },
  "news": {
    "sectionTitle": "市場新聞及AI分析",
    "impact": {
      "bullish": "看漲",
      "neutral": "中性",
      "bearish": "看跌"
    },
    "analysisUnavailable": "AI分析暫時無法使用",
    "noArticles": "暫無相關新聞",
    "loading": "正在載入新聞...",
    "error": "新聞數據暫時無法使用",
    "retry": "重試",
    "source": "來源"
  },
  "common": {
    "lastUpdated": "最後更新：{time}",
    "staleWarning": "數據可能已過時",
    "price": "價格",
    "change24h": "24小時變化"
  }
}
```

Merge these into the existing JSON objects. If a "common" key already exists, merge into it (don't replace).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/pennyvest/v1 && node -e "const en=require('./src/messages/en.json'); const zh=require('./src/messages/zh-HK.json'); const checks=['charts.allocationTitle','charts.simulatedLabel','news.sectionTitle','news.impact.bullish','common.lastUpdated','common.price']; for(const k of checks){const parts=k.split('.'); let ev=en,zv=zh; for(const p of parts){ev=ev?.[p];zv=zv?.[p];} if(!ev||!zv){console.error('MISSING:',k);process.exit(1);}} console.log('All i18n keys present');"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "allocationTitle" src/messages/en.json
    - grep -q "simulatedLabel" src/messages/en.json
    - grep -q "sectionTitle" src/messages/en.json
    - grep -q "bullish" src/messages/en.json
    - grep -q "lastUpdated" src/messages/en.json
    - grep -q "staleWarning" src/messages/en.json
    - grep -q "資產配置" src/messages/zh-HK.json
    - grep -q "模擬回測" src/messages/zh-HK.json
    - grep -q "看漲" src/messages/zh-HK.json
    - grep -q "最後更新" src/messages/zh-HK.json
  </acceptance_criteria>
  <done>Both en.json and zh-HK.json contain all Phase 3 keys: charts (allocationTitle, equityTitle, simulatedLabel, loading, error, retry), news (sectionTitle, impact badges, analysisUnavailable, noArticles, loading, error, retry, source), common (lastUpdated, staleWarning, price, change24h). No existing keys removed.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes (chart components compile)
- Both chart components have 'use client' directive
- AllocationDonut uses PieChart with innerRadius (donut)
- EquityCurve has "Simulated back-test" label visible
- All i18n keys exist in both en.json and zh-HK.json
- JSON files parse without errors
</verification>

<success_criteria>
- Donut chart component renders allocation breakdown with center hole per D-01
- Equity curve component renders area chart with simulated label per D-02
- All Phase 3 UI text has bilingual translations
- Chart components are ready to receive data from Route Handlers
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-data-charts-and-ai/03-03-SUMMARY.md`
</output>
